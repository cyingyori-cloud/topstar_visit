## 「拜访目标」→「推荐拜访原因」重命名与文案改写

- **标题改名**：所有出现"拜访目标"的地方改为"推荐拜访原因"（TaskCard、ConfirmVisitModal、PendingConfirmationSection、VisitDetailModal）
- **visitGoal 文案改写**：8条 visitGoal 从行动指令（如"确认采购节奏，约定方案评审时间"）改为推荐理由风格（如"距上次拜访仅3天且商务进展顺利，当前处于采购决策窗口期，趁热推动可缩短成交周期"），去掉"采购节奏""高层决策"等 action-oriented title
- **布局调整**：将"推荐拜访原因"从左侧移到任务卡片底部左下角，将"推进重点/线索目标"放在底部右下角，形成整张卡片底部双栏而不是右侧窄栏内嵌
- commit: 4833026（文案）、8018e0c（初版布局）、a04571b（最终底部双栏修正），push 超时待重试

## 顶部角色视角修复

- **默认角色修复**：`src/stores/appStore.ts` 默认 currentRep 从 `salesReps[0]`（王总/销售总监）改为首个 level=3 一线销售，避免页面初始仍显示销售总监
- **TopNav 清理**：移除顶部导航中销售总监/区域总监图标与颜色映射残留，当前角色与下拉列表统一使用一线销售蓝色样式
- **待确认提醒数据源修复**：默认角色改为一线销售后，`filteredTasks` 会按销售客户池过滤，导致德赛电池 c6 无商机待确认任务被隐藏；已将 PendingConfirmationSection 数据源改为完整 `visitTasks` 的 pending_confirmation，全量显示 3 条（商机进行中2/无商机1），本周拜访任务仍使用 filteredTasks
- commit: a6d631b（角色）、8a9dc51（待确认全量恢复）

## 本周拜访任务数据补充

- **问题**：默认角色改为一线销售后，本周拜访任务按张三客户池过滤，只剩 2 条
- **修复**：新增3条张三负责客户的本周已确认任务：TCL华星（c11，模组段搬运自动化需求澄清）、亿纬锂能（c14，电芯分选自动化场景识别）、蓝思科技（c16，玻璃上下料机器人方案补充）
- **结果**：visitTasks 从8条增至11条，一线销售视角下本周任务更饱满
- commit: 2c5c650

## 任务详情弹窗信息结构调整

- **详情弹窗重构**：移除原先商机情况中的 Markdown 表格文本，改为与任务卡片一致的结构化展示：客户级别/商机状态、拜访主题、对象、地点、历史拜访、商机信息/机会识别、客户健康度、推荐拜访原因、推进重点/线索目标、上次拜访情况
- **语义样式**：推荐拜访原因使用暖色底，推进重点使用浅灰底，健康度使用分数对应颜色
- commit: 99af906

# 2026-05-15 工作日志

## 拜访任务模块 UI 批量优化（7条需求）

- **待确认提醒与本周任务分栏独立**：两个区块本已分离，确认现有结构正确
- **待确认提醒第三行加「拜访目标：」前缀**：`visitGoal` 前加了带样式的标签
- **确认拜访弹窗加入拜访目标字段**：新增可编辑 textarea，预填 AI 建议的 `task.visitGoal`，提交时同步传递
- **超期规则修正**（与用户提供图片对齐）：
  - A级：30天 → 7天（1次/周）
  - B级：60天 → 30天（月度联系）标签改为「活跃客户」
  - C级：90天 → 30天（至少1次触达/月）标签改为「沉睡客户」
- **任务卡片商机情况精简**：去掉多格 InfoPair 表格，改为两句话简述（主商机+阶段+进度）
- **三大模块去掉顶部一句话概括**：客户盘点、拜访赋能、经营复盘顶部描述卡片全部移除
- **SABC分层规则改左右布局**：从 4列 grid 改为竖向列表，每行左侧级别标签+右侧规则文字，节省垂直空间

## 纠正：拜访任务分栏独立

- 用户指出“待确认拜访提醒”和“本周拜访任务”仍在同一个拜访任务栏目内。已修正 `VisitTasks.tsx`：将待确认提醒从拜访任务卡片内部拆出，成为一个独立白色卡片栏目；下方卡片标题改为“本周拜访任务”，只展示已确认/常规任务。构建通过。

## 确认拜访弹窗顺序调整

- 删除"推荐拜访原因"区块（用户不需要在弹窗里看这段AI生成文本）
- 表单顺序改为：拜访日期/时间 → 拜访联系人 → 拜访目标（AI预填可改）→ 补充说明
- 补充说明 rows 从3改为2，弹窗整体更紧凑

## 本周拜访任务卡片宽短化

- 用户反馈任务卡片仍过长。已将本周任务列表从 3 列改为 2 列，使卡片更宽；单卡内部改为左右布局，顶部合并标签/时间/距上次信息，拜访目标与商机情况限制两行展示，完成度标签与按钮压到底部同一行。Lint 与构建均通过。

## 客户列表改卡片网格 + 数据真实化

- **客户列表从列表改为卡片网格**：`CustomerManagement.tsx` 中客户列表从 `divide-y` 列表改为 `grid grid-cols-2` 双列卡片布局
- **客户数 10→30**：`mockData.ts` 中新增 20 家客户（S级4家、A级6家、B级10家、C级10家）
- **商机总额降至约 300 万**：S级 50~120 万、A级 20~60 万、B级 8~25 万、C级 3~10 万
- **数据贴合 SABC 分层规则**：
  - S级：大商机、高频拜访（7天超期）、面板/3C/家电等核心行业
  - A级：中等商机、每周拜访（7天超期）、面板/精密件/锂电
  - B级：小商机、月度联系（30天超期）、LED/电池/玻璃等
  - C级：极小商机、激活优先（30天超期）、沉睡状态
- **拜访记录同步更新**：`completedVisits` 按新30家客户调整，确保超期/合规状态真实
- **销售代表客户列表同步**：`roles.ts` 中 rep0/rep1 包含全部30家，rep2/rep3 分配覆盖
- **公司名映射补充**：`companyNames.ts` 新增 TCL华星、大族激光、深天马、亿纬锂能等映射

## 修正超期天数 999→真实数据

- **根因**：`getDaysSinceLastVisit()` 在无拜访记录时返回硬编码 999，导致客户卡片显示"严重超期999天"
- **CustomerManagement.tsx 修正**：
  - 返回值从 `number` 改为 `{ days, hasVisit }`，无拜访时 `hasVisit=false`
  - 标签逻辑：从未拜访 → 显示灰色「未拜访」标签，不再计算超期
  - 底部天数文案：`hasVisit ? 'X天前' : '从未拜访'`
- **C级沉睡客户补全拜访记录**（10家全部补充电话/微信触达，日期分布在 2025-12 ~ 2026-04，超期 27~146 天）
- **B级无拜访客户补全记录**（木林森 4/8、信利光电 3/22、伯恩光学 4/25、领益智造 3/10、欧菲光 4/15、合力泰 2/20）
- **coverageData 同步调整**：B级覆盖 4→6，C级覆盖 2→8，总覆盖 14→22；uncoveredHighPriority 替换为真实超期客户

## SABC 分层卡片化 + 客户列表化

- **SABC 分层规则改卡片**：从左右横排列表改为 `grid-cols-4` 四张独立卡片，每张顶部有对应颜色边框，内含级别标签、拜访频率、超期天数
- **客户数据从卡片网格改回列表**：改为 `table` 表格形式，列包括客户名称、级别、地区、行业、商机、联系人、拜访状态，hover 高亮行
- 清理未使用的 lucide 图标 import（AlertTriangle, Clock, Star, Target）

## 本地页面样式恢复

- 用户反馈页面显示为纯文本/布局乱掉。排查发现本地 5177 端口服务返回异常，Tailwind CSS 未正常注入，导致样式整体失效。
- 未修改业务源码；操作为：杀掉异常 Vite 进程、清理 `.vite` 与 `node_modules/.vite` 缓存、用 `npm run dev -- --host 127.0.0.1 --port 5177` 重启本地服务。
- 验证 `/src/index.css` 已返回 Tailwind 编译结果（包含 `.flex`、`.grid`、`.p-4` 等关键类），并重新打开本地预览。

## 经营复盘「本周经营洞察」卡片

- 用户反馈经营复盘页面内容空、缺乏逻辑感。在 `Statistics.tsx` 中新增「本周经营洞察」三栏卡片：
  - **超期客户**（黄底）：统计各层级超期客户数量、涉及商机金额，按 S/A/B/C 分色标签展示
  - **本周拜访**（蓝底）：本周任务总数 + 待确认数量
  - **商机推进**（绿底）：方案沟通 + 方案评估阶段客户数
- 数据基于 `TIER_RULES` 和 `completedVisits` 实时计算，非硬编码
- 新增时间维度切换按钮组：本日 / 本周 / 本月 / 本季度，切换后实时重新计算洞察数据（超期判断始终基于当前时间，不受维度切换影响）

## 拜访任务卡片标签上移

- 用户要求拜访类型标签（如"技术交流"）放在卡片顶部 title 旁边，而非中部标签行
- `VisitTasks.tsx` 调整：将 `task.visitType` 标签从卡片中部标签行移到顶部标题行，紧跟公司名称右侧
- 中部标签行保留：超期/合规状态、时间、距上次天数，减少重复占用

## 销售成单路径阶段统一替换

- 用户指出所有商机阶段必须参照其提供的「销售成单路径」图，禁止自行编造阶段名
- 将 `mockData.ts` 中全部 30 家客户的 `opportunityStage` 替换为销售路径 10 阶段 + 休眠：
  - S级：华星"获"商机(10%)、富士康"约"高层(80%)、立讯"查"信息(10%)、格力"定"商务(95%)
  - A级：比亚迪"做"客情(30%)、长盈"收"线索(0%)、欣旺达"约"高层(80%)、TCL华星"获"商机(10%)、大族激光"做"客情(30%)、深天马"收"线索(0%)
  - B级：德赛"收"线索、木林森"查"信息、瑞声"获"商机、亿纬锂能"查"信息、信利"收"线索、蓝思"做"客情、伯恩"查"信息、领益"收"线索、欧菲光"查"信息、合力泰"收"线索
  - C级：全部"休眠"(0%)
- `Statistics.tsx` 同步更新：阶段列表改为 11 项（10 阶段 + 休眠），商机推进卡片统计改为「线索/商机/高层」维度

## GitHub 上传与 Pages 部署

- 用户要求上传代码到 `git@github.com:cyingyori-cloud/topstar_visit.git` 并返回可访问系统地址。
- `main` 分支源码已与 `origin/main` 保持一致；重新执行 `npm run build` 成功。
- 使用独立 worktree 将最新 `dist/index.html` 发布到 `gh-pages` 分支并推送成功：`1caf45a..657799d gh-pages -> gh-pages`。
- 验证线上地址 `https://cyingyori-cloud.github.io/topstar_visit/` 返回 HTTP 200，页面包含 `TopStar Visit AI` 与内联 `<style>`，已打开预览。

## 客户全称 + 拜访标签样式

- **companyNames.ts 补全 30 家客户全称**：新增 TCL华星、大族激光、深天马、亿纬锂能、信利光电、蓝思科技、伯恩光学、领益智造、欧菲光、合力泰、京东方、天马微电子、维信诺、柔宇科技、汇顶科技、兆易创新、韦尔股份、闻泰科技、龙旗科技、华勤技术等映射，覆盖 S/A/B/C 四级全部客户
- **拜访任务卡片互动方式标签恢复样式**：将 ✓/… 文字前缀替换为 `CheckCircle2` 图标，已完成的用绿色圆圈勾，未完成的用灰色空心圆圈，辨识度更高

## 待确认拜访提醒：有商机/无商机差异化

- 用户指出待确认提醒需要体现有商机与无商机拜访目的/内容差异
- `mockData.ts` 为待确认任务新增 `opportunityIntent` 与 `visitFocus` 字段：
  - 华星光电、长盈精密：`with_opportunity`，拜访目的聚焦商机推进、预算/技术节点确认
  - 德赛电池：`no_opportunity`，拜访目的聚焦首次建联、痛点摸排、机会识别
- `VisitTasks.tsx` 待确认提醒顶部新增统计：有商机 X / 无商机 X
- 每条待确认提醒新增「有明确商机/无明确商机」标签，并以两栏展示：
  - 有商机：拜访目的=推进商机，本次内容=确认节点
  - 无商机：拜访目的=识别机会，本次内容=建立关系
- Lint 与构建通过，已打开本地预览。

## 机器人手臂销售话术专业化

- 用户指出不要出现“实施”字眼，且企业是卖机器人手臂，需要改为制造业通用专业话术
- 全项目搜索并替换 `实施`、`注塑岛`、`实施节奏`：确认已清零
- `mockData.ts`：
  - 格力商机 `注塑岛` → `注塑上下料工作站`
  - 拜访目的 `注塑岛方案汇报` → `注塑上下料工作站方案沟通`
  - 拜访目标改为：`让客户认可机器人上下料工作站的总体方案、节拍匹配与导入路径，推动进入商务评估。`
  - 历史拜访记录改为围绕设备节拍、夹具方式、产线衔接边界、人工转运痛点
- `skills.ts`：案例产品 `注塑岛` → `注塑上下料工作站`，状态 `待交付` → `商务评估`
- 知识库话术中“注塑车间的自动化改造”改为“注塑车间引入机器人上下料工作站”
- Lint 和 build 通过，已打开本地预览。

## 拜访任务商机/健康度批量优化

- 根据用户8条要求继续优化 `VisitTasks.tsx`、`CoverageRate.tsx`、`AddVisitModal.tsx`、`mockData.ts`
- **标签文案**：`有明确商机` 改为 `商机进行中`；无商机统一显示 `无商机`，清理旧文案 `Q3设备采购跟进`、`自动化产线方案沟通`、`初次触达`、`暂无明确商机`
- **待确认提醒**：标题栏改深色底；`商机进行中 2` / `无商机 1` 放在“待确认拜访提醒”标题后，不再放右端
- **本周拜访任务**：标题栏改深色底；每张任务卡加入客户健康度评分（总分/100、趋势、危险/预警/健康、进度条）
- **本周任务商机差异化**：卡片顶部显示 `商机进行中` / `无商机`；商机进行中卡片在拜访主题上方突出展示 `商机名称`、`商机金额`、`商机阶段`、`商机风险`；无商机卡片展示机会识别/线索培育重点
- **确认拜访弹窗**：在拜访目标前新增 `商机主题` 下拉字段；有商机任务提供具体商机选择，无商机任务提供线索培育/机会识别/痛点摸排选项
- **拜访覆盖率**：标题栏同步改深色底
- Lint、指定文案搜索与 build 均通过，已打开本地预览。

## 角色视角与拜访目标展示优化

- 用户要求切换角色视角不要出现高层和中层，仅保留一线销售
- `TopNav.tsx` 中角色切换下拉从 `[1,2,3]` 改为仅展示 `[3]`，分组标题固定为“一线销售”
- 用户指出拜访目标框内文字不能出现排不下和省略号；`VisitTasks.tsx` 去掉拜访目标的 `line-clamp-2`，改为完整展示
- 同时压缩 `mockData.ts` 中 8 条拜访任务 `visitGoal` 文案，保留核心动作，避免卡片过高或内容拥挤
- Lint 与 build 通过，已打开本地预览。
